# 원자료 안내

이 폴더의 인터뷰 원자료(`*.txt`, `*.docx`)는 **개인정보 보호를 위해 공개 저장소에서 제외**되어 있습니다.

`analyze.py`를 직접 실행하려면 아래 파일을 이 폴더에 배치해야 합니다.

- `eco_review_final_test0520.txt` — 응답자 20명(A~T) × 질문 4개(Q1~Q4) 인터뷰 응답

집계 결과(키워드·빈도·의미연결망)는 개인 식별 정보를 포함하지 않으며,
`public/data/report.json` 에 저장되어 있습니다.
